mutation registerFacebookUser($data: CreateFacebookUserInput!) {
    registerFacebookUser(data: $data) {
      user {
        id
        staff
        firstname
        lastname
        email
      }
      profile {
        id
        profile_avatar
        profile_cover
      }
      accessToken
      refreshToken
    }
  }
